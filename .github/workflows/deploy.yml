name: 🚀 Deploy to EC2 (Production)

on:
  push:
    branches:
      - production

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@v4

      - name: 🛠️ Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 📦 Build app
        run: ./gradlew clean build

      - name: 📁 Upload JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "build/libs/syncora.jar"
          target: "~/syncora-prod"

      - name: 🔁 Restart Spring Boot (systemd)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            sudo systemctl stop syncora.service || true
            sudo sed -i "s|^Environment=JWT_SECRET=.*|Environment=JWT_SECRET=${{ secrets.JWT_SECRET }}|" /etc/systemd/system/syncora.service
            sudo sed -i "s|^Environment=DB_PASSWORD=.*|Environment=DB_PASSWORD=${{ secrets.DB_PASSWORD }}|" /etc/systemd/system/syncora.service
            sudo systemctl daemon-reload
            sudo systemctl start syncora.service
            sudo systemctl status syncora.service --no-pager
